
[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "poseigen_compass"
version = "0.1.3"
description = "Hyperparameter optimization and model evaluation utilities for the Poseigen family"
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.12"
license = "MIT"
license-files = ["LICENSE.txt"]
authors = [{ name = "Husam Abdulnabi", email = "husam.abdulnabi@gmail.com" }]
maintainers = [{ name = "Husam Abdulnabi", email = "husam.abdulnabi@gmail.com" }]
keywords = ["poseigen", "bioinformatics", "machine-learning", "hyperparameter-optimization"]

classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "Topic :: Scientific/Engineering",
  "Topic :: Software Development",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
]

dependencies = [
  "numpy",
  "pandas",
  "poseigen_seaside",
]

[project.optional-dependencies]
dev = [
  "build",
  "twine",
  "pytest",
]

[project.urls]
Homepage = "https://github.com/husam94/poseigen_compass"
Repository = "https://github.com/husam94/poseigen_compass"
Issues = "https://github.com/husam94/poseigen_compass/issues"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]